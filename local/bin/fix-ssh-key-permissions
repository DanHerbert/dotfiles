#!/bin/bash
shopt -s extglob

set -x

primary_group=$(id -gn "$USER")
sudo chown "$USER":"$primary_group" "$HOME"/{.ssh,.ssh/keys}/id_*
sudo chmod 600 "$HOME"/{.ssh,.ssh/keys}/id_!(*\.pub)
sudo chmod 644 "$HOME"/{.ssh,.ssh/keys}/id_*.pub

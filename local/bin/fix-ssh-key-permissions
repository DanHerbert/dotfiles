#!/bin/bash
shopt -s extglob

set -v
primary_group=$(id -gn "$USER")
sudo chown -fc "$USER":"$primary_group" "$HOME"/{.ssh,.ssh/keys}/id_*
sudo chmod -fc 600 "$HOME"/{.ssh,.ssh/keys}/id_!(*\.pub)
sudo chmod -fc 644 "$HOME"/{.ssh,.ssh/keys}/id_*.pub
